{
  "name": "connect",
  "version": "0.1.0",
  "description": "Connecti monorepo for the fullstack project",
  "private": true,
  "workspaces": [
    "apps/*",
    "web/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w @connecti/api\" \"npm run dev:socket -w @connecti/api\" \"npm run dev -w @connecti/web\"",
    "build": "npm run build -w @connecti/shared && npm run build -w @connecti/api && npm run build -w @connecti/web",
    "lint": "npm run lint --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "typecheck": "npm run build -w @connecti/shared && npm run typecheck --workspaces --if-present",
    "dev:api": "npm run dev -w @connecti/api",
    "dev:socket": "npm run dev:socket -w @connecti/api",
    "dev:web": "npm run dev -w @connecti/web"
  },
  "devDependencies": {
    "@types/google.accounts": "^0.0.18",
    "concurrently": "^9.2.0",
    "eslint": "10.9.1",
    "typescript": "6.0.3",
    "typescript-eslint": "8.68.0"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
